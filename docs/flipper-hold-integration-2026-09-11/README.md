# 翅膀正式素材与播放减速

用户确认 `preview-v4-color` 可作为正式素材，并要求稍慢。已接入左右各 16 帧握手循环与 12 帧不耐烦，合计 56 帧。

握手一轮由 3.22 秒改为 **4.25 秒**，保留原动作的长短停顿；不耐烦由 1.5 秒改为 **2.04 秒**，每帧 170 ms。400 ms 开始长按循环，按下后 5–10 秒随机触发一次不耐烦。提前松手／取消／拖动／新会话／销毁控制器会停止握手；已触发的不耐烦在松手后继续播完。

导出按共同基线与比例处理，企鹅名义高度仍为 198 逻辑像素，落脚基线 215。左侧伸手最远帧需要额外留白，因此透明画布由 320×228 扩到 368×228；企鹅和原 220×228 触摸区域尺寸不变，默认位置继续居中，120% 窗口上限同步增加。旧动作继续使用原资源与锚点。左侧握手首次正式导出时完成紫色边缘清理，透明度不变。

验证：40 项触摸回归检查通过；播放器测试验证正式左右逐帧时长、循环和单次完成；TypeScript 与 Vite 构建通过（已有大文件块提示）；56 张 WebP 均完整、无画布裁切。浏览器运行前端，以真实鼠标长按验证两侧，反应分别在 8.556／8.553 秒触发，无页面异常与失败资源。录像为真实前端渲染，不是原生窗口录屏。

- [运行演示](demo.html)
- [左侧 GIF](left-runtime-demo.gif) / [右侧 GIF](right-runtime-demo.gif)
- [正式运行素材目录](../../public/assets/animations/hd/)
- 本机源帧与素材包：`output/flipper-identity-unification-2026-09-11/final-approved/`、`flipper-actions-final.zip`，按项目约定不纳入 Git。
- 原代码、配置、反应资源与 exe 备份：`backup/`。

复现安装：`scripts/install-approved-flipper-actions.py`；录制：`scripts/record-flipper-hold-demo.cjs`。未改变工作网络或代理。

Windows 发布构建已成功，根目录 `desktop-pet.exe` 已更新，与构建输出 SHA256 一致。旧 exe 保留在备份中；未自动启动。构建仅有原有资源块大小提示与链接器创建导入库提示。
