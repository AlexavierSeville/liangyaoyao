# 摸翅膀长按接入进度

- [x] 右侧 16 帧素材已获用户定稿授权并打包。
- [x] 现有长按交互梳理：400 ms 判定长按，身体不耐烦在按下后 5–10 s 触发一次。
- [x] 播放器支持可选 `frameDurationsMs`，保留原有 `fps` 回退；非法时长在加载前拒绝。
- [x] 新的逐帧时长测试通过；原有 25 项触摸交互检查通过；TypeScript 与 Vite 构建通过。
- [x] 左侧初稿及局部回环修订，形成完整 16 帧审核预览；失败的生图修订弃用，末帧复用同组首帧轻抚，释放帧平移现有手层。
- [ ] 用户确认左侧新素材后，统一左右正式资产、反应素材比例和运行时配置。
- [ ] 加入 `FlipperHold` 循环动作，长按按左右侧选择循环；松开、取消、拖动、新会话和释放控制器时停止循环。
- [ ] 不耐烦动作抢占循环，释放鼠标不提前打断已触发的不耐烦；验证 stale completion 不重启循环。
- [ ] 实际程序左右两侧连贯演示、回归测试并更新 exe。

用户最初明确要求新绘制初版经其同意后继续。当前制作左侧初稿，并独立完成播放器必要准备；正式配置及 exe 仍未切换。播放器改动前文件保存在 `backup/`。

逐帧时长测试脚本：`scripts/test-frame-durations.cjs`。覆盖 16 帧 3.22 秒精确时序、多个循环/大步长、旧 FPS 动作、单次结束、非法时长与完成回调切换动作。

接入时的配置约定：左右 `touch_flipper_hold_screen_left/right`，16 帧，`frameDurationsMs=[220,180,160,260,180,220,180,200,180,180,220,180,180,220,200,260]`，`loop=true`，过渡建议 160 ms，动作 `FlipperHold`，优先级 50。已有 `touch_flipper_react_screen_left/right` 继续负责长按不耐烦反应。

左侧来源通过内置 imagegen 分两批生成，保留每批提示词和原始生成图；后半段再做第 13 帧释放间隙及第 16 帧降低翅膀的最小修订。未调用第三方图片 API，未改动网络设置。


## 2026-09-11 已完成

用户确认统一颜色版正式发布，已完成左右循环／不耐烦接入并放慢至 4.25 秒／2.04 秒。正式发布结果、40 项交互测试、实际前端录像和 exe 校验详见 `../flipper-hold-integration-2026-09-11/`。上方原清单为此前阶段记录。
